"""
Конфигурация RAG-системы.
Все настройки в одном месте — легко менять модель, адрес Qdrant и параметры.
"""

import os
from pathlib import Path
from dotenv import load_dotenv

load_dotenv(Path(__file__).resolve().parent.parent / ".env")

# ── Пути ─────────────────────────────────────────────────────────────
PROJECT_ROOT = Path(__file__).resolve().parent.parent
DATA_DIR = PROJECT_ROOT / "data"
CHUNKS_PATH = DATA_DIR / "chunks" / "chunks.json"
DOC_TEXTS_PATH = DATA_DIR / "chunks" / "doc_texts.json"

# ── Модель эмбеддингов ──────────────────────────────────────────────
EMBEDDING_MODEL_NAME = "ai-forever/ru-en-RoSBERTa"
EMBEDDING_DIMENSION = 1024  # размер вектора для этой модели

# ── Qdrant ───────────────────────────────────────────────────────────
QDRANT_HOST = "localhost"
QDRANT_PORT = 6333
COLLECTION_NAME = "department_chunks"

# ── Чанковка ─────────────────────────────────────────────────────────
CHUNK_SIZE = 500
CHUNK_OVERLAP = 50

# ── Retriever ────────────────────────────────────────────────────────
TOP_K = 3  # сколько документов (страниц) возвращать при поиске
SEMANTIC_TOP_K = 15  # сколько чанков брать из семантического поиска (до дедупликации)
KEYWORD_TOP_K = 10  # сколько чанков брать из keyword-поиска

# ── Agent ────────────────────────────────────────────────────────────
AGENT_MAX_ITERATIONS = 3  # максимум вызовов инструмента поиска

# ── LLM (OpenRouter) ────────────────────────────────────────────────
YANDEX_CLOUD_API_KEY  = os.getenv("YANDEX_CLOUD_API_KEY", "")
YANDEX_CLOUD_FOLDER = os.getenv("YANDEX_CLOUD_FOLDER", "")
BASE_URL = "https://ai.api.cloud.yandex.net/v1"
LLM_MODEL = "deepseek-v32/latest"
LLM_TEMPERATURE = 0
LLM_MAX_TOKENS = 2048

SYSTEM_PROMPT = """\
Ты — интеллектуальный помощник кафедры аэрогидромеханики КФУ. Будь краток и содержателен в своем ответе. Веди себя профессонально.
У тебя есть инструмент search_documents для поиска информации по базе данных кафедры.
Не используй поиск без необходимости, если ты уже нашел подходящий ответ, то выдавай его пользователю.

Как работать:
1. Получив вопрос пользователя, сформулируй подходящий поисковый запрос и вызови search_documents.
Правило: если ты ищешь запрос в первый раз, отправь его в поиск без изменений. 
2. Запрос для поиска может отличаться от вопроса пользователя — выбери ключевые слова, \
которые лучше всего помогут найти нужную информацию. Не пиши 
3. Если найденные документы не содержат ответа, попробуй переформулировать запрос и поискать ещё раз.
4. Основывай ответ ТОЛЬКО на найденных документах. Не придумывай информацию.
5. Указывай источники (URL страниц), откуда взята информация.
6. Отвечай на русском языке.
7. Будь вежлив и помогай пользователям разобраться в информации о кафедре.
8. Не выдавай информации, о которой пользователь не просил. Все четко по существу.
9. В своем последнем ответе ни при каких обстоятельствах не пиши своих рассуждений по поиску (например "О, нашел!", "Отлично, теперь у меня есть информация")

Если после нескольких попыток поиска ты не нашёл релевантную информацию — \
честно скажи, что не удалось найти ответ в базе данных кафедры, \
и предложи пользователю переформулировать вопрос.
"""
